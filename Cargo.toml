[package]
name = "depi"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5.42", features = ["derive"] }
miniserde = "0.1.42"
ureq = "3.0.12"

# tokio = { version = "1" }

[dev-dependencies]
tempdir = "0.3.7"
